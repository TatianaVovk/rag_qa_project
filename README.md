
# RAG-QA: Вопрос-Ответ сервис на базе LLM и векторного поиска

## 📌 Описание проекта

Проект представляет собой прототип сервиса вопрос-ответ (Q&A) с архитектурой Retrieval-Augmented Generation (RAG). Он сочетает возможности векторного поиска и большой языковой модели (LLM), чтобы давать ответы на основе предоставленной базы знаний.

## 💬 Примечания

Так как у меня не слишком мощное железо, а векторизация с помощью модели `intfloat/multilingual-e5-base` и генерация ответов с использованием LLM (`Mistral-7B-Instruct`) ресурсоемки, я использую мощность Google Colab:

- LLM запускается в Colab, а не локально — это экономит ресурсы.
- Ngrok URL временный, но автоматически подставляется в `.env`. Нужно лишь скопировать значение из `llm_env.env`, сгенерированного в Colab, в такой же файл в проекте.

### Особенности

- Индексация и очистка пользовательских текстов
- Векторизация через `intfloat/multilingual-e5-base`
- Векторная база данных FAISS
- Генерация ответов с помощью LLM (через ngrok API)
- REST API на FastAPI

---

## 📁 Структура проекта

```
RAG_QA_PROJECT/
├── api/
├── configs/
├── data/
├── indexing/
├── indexing_service/
├── notebook/
├── query_service/
├── vector_db/
├── venv/
├── .dockerignore
├── .env
├── docker-compose.yml
├── Dockerfile.api
├── Dockerfile.indexing
├── llm_env.env
├── rag_qa_project.rar
├── README.md
├── requirements.txt
└── update_env_and_run.py
```

---

## 📊 Архитектурная схема

```
┌────────────┐       Индексация      ┌────────────────┐
│  JSON-файл │ ───────────────────▶ │ cleaner.py     │
└────────────┘                       │ data_loader.py │
                                     └──────┬─────────┘
                                            ▼
                                   ┌───────────────────┐
                                   │ Векторизация      │
                                   │ (multilingual-e5) │
                                   └──────┬────────────┘
                                          ▼
                                ┌────────────────────┐
                                │ FAISS-векторная БД │
                                └──────┬─────────────┘
                                       ▼
                       ┌────────────────────────────────┐
                       │       FastAPI (Query API)      │
                       │ build_prompt.py, search_engine │
                       └──────┬────────────┬────────────┘
                              │            │
      ┌───────────────────────┘            ▼
      ▼                               ┌───────────────┐
Input Query (от пользователя)         │ LLM via ngrok │
POST /query                           │ (Mistral-7B)  │
      ▲                               └──────┬────────┘
      │                                      │
      └────────────  Ответ в формате JSON ◀──┘

```

---

## ⚙️ Как развернуть проект

> ❗ LLM работает в Colab, API — локально через Docker.
> ❗Зайдите на сайт https://dashboard.ngrok.com и зарегистрируйтесь. после чего вы получите ваш токен который нужно будет вставить в файл llm_generation_via_api.ipynb 
> ❗Так же вам нужен токен от HuggingFace, который необходимо вставить в файл config.py, в переменную HF_API_TOKEN, получить его можно следующим образом:
* Перейдите на https://huggingface.co/settings/tokens
* Нажмите “New token”
* Назовите и выберите права read
* Скопируйте токен и вставьте в в файл config.py

> ❗ Генерация эмбеддингов перед запуском

В репозиторий **не включены** следующие файлы из-за их размера:
- `vector_db/chunk_embeddings.npy`
- `vector_db/chunk_ids.json`

Перед началом проекта вам необходимо сгенерировать их самостоятельно, для этого вам нужно запустить файл generate_embeddings.py, после чего в папке vector_db/ появятся, нужные файлы:
* chunk_embeddings.npy — векторные представления текстов
* chunk_ids.json — соответствующие им идентификаторы

### Шаг 1: Запуск модели в Colab

1. Откройте `llm_generation_via_api.ipynb` в Google Colab
2. Вставьте ваш токен в conf.get_default().auth_token = "Ваш токен" 
3. Выполните все ячейки. По завершении будет сгенерирован `llm_env.env`

### Шаг 2: Подставьте ссылку в проект

Возьмите новую ссылку из файла `llm_env.env` в colab и поместите ее в файл с этим же нозванием в проекте, по этому примеру:
В файле в colab вы увидите это:
COLAB_LLM_URL=NgrokTunnel: "https://1252-34-106-202-122.ngrok-free.app" -> "http://localhost:7860"/generate

Тогда в файле проекта у вас должно быть так:
COLAB_LLM_URL=https://1252-34-106-202-122.ngrok-free.app/generate
USE_MOCK_LLM=False

### Шаг 3: Запуск API через Docker

1. Убедитесь, что Docker запущен  
2. Выполните:
```bash
python update_env_and_run.py
```
3. Перейдите по ссылке:
👉 [http://127.0.0.1:8000/docs](http://127.0.0.1:8000/docs)

---
 💡 **Примечание для пользователей с мощным оборудованием**

 Если у вас достаточно мощное железо, вы можете не использовать ресурсы Google Colab. Все шаги инструкции остаются актуальными, за исключением следующего:

 - Установите LLM (`Mistral-7B-Instruct`) локально в рамках данного проекта.
 - Внесите изменения в файл `llm_generation_via_api.ipynb`, чтобы сохранить `llm_env.env` локально вместо Colab.
---

## 🛠️ Зависимости

```txt
fastapi
uvicorn[standard]
transformers
sentence-transformers
faiss-cpu
pydantic
python-dotenv
requests
ctransformers
torch
```

---
## 📊 Результаты базового анализа данных и проверка качества

На этапе предварительного анализа и очистки данных были выполненно следующие:

### 🔎 Проверка качества и дубликатов

- **Пустых и null-значений** в поле `text` — не обнаружено.
- **UID уникальны** и могут использоваться в качестве идентификаторов.
- Обнаружено **126 дубликатов по `text`**, большинство из которых — короткие или технические фрагменты.

### 🧹 Очистка и фильтрация

- **Минимальная длина текста ≥ 20 символов** — фильтрация коротких строк работает корректно.
- **Дубликаты успешно удалены.**
- **Битый или нестандартный юникод** эффективно удаляется регулярным выражением внутри `clean_text()` — при этом не затрагиваются полезные элементы (например, транскрипции, символы валют, арабский или иврит).
- **Короткие тексты не являются шумовыми** — сохраняют смысл и ценность.
- **Распределение длины** текстов адекватно, без экстремальных выбросов.

### 📌 Расширенные проверки

- **Уникальных wiki-страниц** (по `ru_wiki_pageid`) — **9105**, в среднем **6 чанков на статью**.
- **Частотный анализ** показывает, что тексты содержательные (топ-слова: *"года"*, *"году"*, *"после"*, *"также"*, *"время"*).
- Обнаружено **189 текстов с битым или нестандартным юникодом** — они успешно очищаются и **не приводят к потере полезных данных**.


## 🚀 Возможности улучшения и масштабирования

Проект задуман как прототип, но его архитектура позволяет легко развивать функциональность. Возможные направления улучшения:

### 🔧 Функциональные улучшения
- **Добавить юнит-тесты и e2e-тестирование**: покрытие ключевых модулей тестами (поиск, генерация, API).
- **Поддержка нескольких языков**: добавить возможность работы с многоязычными корпусами.
- **Асинхронная генерация**: интеграция очередей задач (например, Celery + Redis) для масштабируемой генерации ответов.
- **Интерфейс пользователя (UI)**: создать веб-интерфейс для удобного использования сервиса.
- **Улучшение prompt engineering**: адаптивные подсказки в зависимости от запроса и контекста.

### 🧠 Качество моделей
- **Обновление модели эмбеддингов**: можно протестировать более лёгкие или доменно-специфичные модели (например, BGE, E5-small).
- **Интеграция более мощной LLM**: при наличии ресурсов — запуск Llama 3, Mistral 7B на сервере или через API (OpenRouter, Together.ai и пр.).

### 🗃 Расширение базы знаний
- **Автоматическая загрузка и индексация новых документов**

### ⚙️ Инфраструктура и масштабирование
- **Переход на прод-сервер (Gunicorn/Uvicorn)** 
- **Docker-оптимизация**: уменьшение размера образов, разделение зависимостей.
- **Поддержка нескольких пользователей и авторизации** — если сервис станет публичным.

## 📷 Скриншоты проекта прилагаются:
Пример запроса к API с вопросом "Когда появилась первая ЭВМ" 
Запрос "Кто такой Гагарин" с извлечением контекста 
Консольный лог запуска с загрузкой эмбеддингов 
Запуск генерации в Colab через ngrok 
